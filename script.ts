/**
 * ? Installer tyspcript en local avec la commande
 * ? npm i -g typescript
 * ? Utiliser la commande tsc script.ts pour lancer le typescript et ajouter -w pour voir
 * ? les modifications en direct
 */

/**
 * ? Le type any nous permet simplement de na pas préciser le type
 * ? via typescript
 *
 * * Peut être utile dans certains cas. Ex : On ne sait pas quel type de
 * * data à récupérer d'une API, d'un formulaire, on souhaite modifier le
 * * le type etc...
 */
let anyData; // any

let anyData: any[] = [];
